<div class="cambio-con">
  <form action="/api/users/reset-password" method="POST" class="container reset-form">
    <h1 class="titles">Restablecer contraseña</h1>
    <div class="mb-3">
      <label for="email" class="label-login">Correo electronico</label>
      <input type="email" class="form-control" name="email" id="email" placeholder="Ingrese su correo electronico"
        required>
    </div>
    <div class="mb-3">
      <label for="token" class="label-login">Codigo de confirmacion</label>
      <input type="text" class="form-control" name="token" id="token" placeholder="Ingrese codigo de confirmacion"
        required>
    </div>
    <div class="mb-3">
      <label for="password" class="label-login">Nueva contraseña</label>
      <input type="password" class="form-control" name="password" id="password"
        placeholder="Ingrese la nueva contraseña" required>
    </div>
    <button type="submit" class="btn-card-ok w-100">Restablecer contraseña</button>
    {{#if error}}
    <p class="text-danger">{{error}}</p>
    {{/if}}
  </form>
</div>